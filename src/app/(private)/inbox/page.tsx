import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inbox",
};

const InboxPage = () => {
  return <h1 className="text-2xl font-semibold text-black">Inbox</h1>;
};

export default InboxPage;
