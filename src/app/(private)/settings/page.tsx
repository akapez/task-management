import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings",
};

const TasksPage = () => {
  return <h1 className="text-2xl font-semibold text-black">Settings</h1>;
};

export default TasksPage;
