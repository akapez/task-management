import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Insights",
};

const InsightsPage = () => {
  return <h1 className="text-2xl font-semibold text-black">Insights</h1>;
};

export default InsightsPage;
