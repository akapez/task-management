import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reports",
};

const ReportsPage = () => {
  return <h1 className="text-2xl font-semibold text-black">Reports</h1>;
};

export default ReportsPage;
